name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.1', '8.2']
    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: lc_advance
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          extensions: mbstring, pdo_mysql, curl

      - name: Wait for MySQL
        run: |
          for i in {1..30}; do
            mysqladmin ping -h 127.0.0.1 -uroot -proot >/dev/null 2>&1 && break || sleep 1
          done

      - name: Import SQL dump and seed test data
        env:
          DB_HOST: '127.0.0.1'
          DB_NAME: 'lc_advance'
          DB_USER: 'root'
          DB_PASS: 'root'
        run: |
          if [ -f sql/lc_advance.sql ]; then
            mysql -h127.0.0.1 -uroot -proot lc_advance < sql/lc_advance.sql
            php scripts/seed_test_data.php
          else
            echo "No SQL dump found, skipping import"
          fi

      - name: Start PHP Built-in Server
        run: nohup php -S 127.0.0.1:8000 -t . > /tmp/php_server.log 2>&1 & sleep 5

      - name: Verify PHP server is running
        run: |
          for i in {1..60}; do
            if curl -s -f http://127.0.0.1:8000/index.php >/dev/null 2>&1; then
              echo "✓ Server ready at http://127.0.0.1:8000"
              break
            fi
            if [ $i -eq 60 ]; then
              echo "✗ Server failed to start"
              cat /tmp/php_server.log
              exit 1
            fi
            echo "Waiting for server ($i/60)..."
            sleep 1
          done

      - name: Run tests
        env:
          TEST_BASE_URL: 'http://127.0.0.1:8000/'
          DB_HOST: '127.0.0.1'
          DB_NAME: 'lc_advance'
          DB_USER: 'root'
          DB_PASS: 'root'
        run: php tests/run_all_tests.php
